/*#include <stdio.h>

int main() {
  //Faca um programa que leia dez conjuntos de dois valores, o primeiro representando o numero do aluno e o segundo representando a sua altura em metros. Encontre o aluno mais baixo e o mais alto. Mostre o numero do aluno mais baixo e do mais alto, juntamente com suas alturas.

  int numAlunos = 10;
  float alturas[numAlunos];
  float mAlto = 0;
  float mBaixo = 0;
  int alunoMaisAlto = 0;
  int alunoMaisBaixo = 0;

   for(int i = 0; i < numAlunos; i++){
     printf("Digite o numero do aluno: ");
     scanf("%f", &mAlto);
   }
   for(int i = 0; i < numAlunos; i++){
     printf("Digite a altura do aluno: ");
     scanf("%f", &mBaixo);
   }
   for(int i = 0; i < numAlunos; i++){
     if(alunoMaisAlto > mAlto){
       mAlto = alunoMaisAlto;
     }
     if(alunoMaisBaixo < mBaixo){
       mBaixo = alunoMaisBaixo;
     }
     printf("O aluno mais alto e o mais baixo sao: %d e %d", alunoMaisAlto, alunoMaisBaixo);
     
   }
}*/